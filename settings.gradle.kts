pluginManagement {
    repositories {
        gradlePluginPortal()
        // Required for snapshot versions
        maven("https://repo.papermc.io/repository/maven-public/")
    }
}

rootProject.name = "punchtree-util"
